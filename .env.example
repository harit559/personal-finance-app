# Environment Variables Example
# Copy this file to .env and customize the values

# Flask secret key (used for session security)
# Generate a random key: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=dev-secret-key-change-me

# Database encryption key
# IMPORTANT: Change this to a strong random key and KEEP IT SAFE!
# If you lose this key, you cannot access your encrypted database
DB_ENCRYPTION_KEY=my-secure-encryption-key-2026

# Database URL (optional, defaults to local SQLite)
# DATABASE_URL=sqlite:///finance.db
